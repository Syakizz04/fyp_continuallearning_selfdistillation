# Slim requirements for the viewer-only dashboard (Streamlit Community Cloud).
# No upper pins, so prebuilt wheels resolve on the Cloud's default Python
# (3.12/3.13). The dashboard reads result CSVs only — no torch / training stack.
# The full training requirements live on `main`.
streamlit>=1.58
plotly>=6.0
pandas>=2.0
numpy>=1.26
