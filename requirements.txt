# Slim requirements for the viewer-only dashboard (Streamlit Community Cloud).
# This branch (dashboard-deploy) deploys dashboard/app.py, which reads result CSVs
# only — no torch / training stack. The full training requirements live on `main`.
streamlit>=1.58
plotly>=6.0
pandas<2.2
numpy<2.0
