# Runtime dependencies for the deployed system (edge_system/).
#
# Kept separate from requirements.txt on purpose:
#   - `main`             carries the heavy training stack (torch, lightning, ...)
#   - `dashboard-deploy` carries a slim viewer-only set for Streamlit Cloud
# Adding these to either would break that split. Install alongside whichever
# one applies:
#
#   pip install -r requirements.txt -r requirements-system.txt
#
# The edge nodes additionally need the training stack from main's
# requirements.txt, because they load and retrain real TFT/PPO checkpoints
# in-process.

fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
redis>=5.2          # shared stock pool; server via `docker compose up -d redis`
psutil>=6.0         # E4 memory-footprint instrumentation
pytest>=8.0
